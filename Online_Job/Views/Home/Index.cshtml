@{
    ViewData["Title"] = "Online Job Portal";
    var username = Context.Session.GetString("Username");
}

@* <!-- Navigation Bar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand fw-bold" href="#">Job Portal</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button> *@
 @*        @{
            var reciterusername = Context.Session.GetString("Username");
            var recruiterUsername = Context.Session.GetString("RecruiterUsername");
        }

        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="/">Job Portal</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    @if (!string.IsNullOrEmpty(username))
                    {
                        <li class="nav-item">
                            <span class="nav-link">Welcome, @username</span>
                        </li>
                        <li class="nav-item">
                            <form method="post" asp-controller="Account" asp-action="Logout">
                                <button type="submit" class="btn btn-outline-danger">Logout</button>
                            </form>
                        </li>
                    }
                    else if (!string.IsNullOrEmpty(recruiterUsername))
                    {
                        <li class="nav-item">
                            <span class="nav-link">Welcome, @recruiterUsername</span>
                        </li>
                        <li class="nav-item">
                            <form method="post" asp-controller="Account" asp-action="Logout">
                                <button type="submit" class="btn btn-outline-danger">Logout</button>
                            </form>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="btn btn-outline-primary mx-1" asp-controller="Account" asp-action="Login">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-outline-success mx-1" asp-controller="Account" asp-action="RecruiterLogin">Recruiter Login</a>
                        </li>
                    }
                </ul>
            </div>
        </nav>

    </div> *@
</nav>

<!-- Hero Section -->
<div class="container-fluid hero-section text-white text-center">
    <div class="overlay"></div>
    <div class="content">
        <h1 class="display-3 fw-bold">Find Your Dream Job</h1>
        <p class="lead">Connecting job seekers with top companies worldwide.</p>
        <a href="/Jobs" class="btn btn-primary btn-lg">Browse Jobs</a>
    </div>
</div>

<!-- Job Search Bar -->
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <form class="search-bar p-3 shadow rounded">
                <div class="row">
                    <div class="col-md-5">
                        <input type="text" class="form-control" placeholder="Job title, skills, or company">
                    </div>
                    <div class="col-md-4">
                        <select class="form-select">
                            <option selected>Choose Location</option>
                            <option>New York</option>
                            <option>San Francisco</option>
                            <option>London</option>
                        </select>
                    </div>
                    <div class="col-md-3 text-center">
                        <button type="submit" class="btn btn-primary w-100">Search</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Custom CSS -->
<style>
    .hero-section {
        background: url('https://source.unsplash.com/1600x900/?office,work') no-repeat center center;
        height: 60vh;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
    }

        .hero-section .overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
        }

        .hero-section .content {
            position: relative;
            z-index: 2;
        }

    .search-bar {
        background: white;
        border-radius: 10px;
    }
</style>
