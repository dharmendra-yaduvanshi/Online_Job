@* @model List<JobPortal.Models.Application>

@{
    ViewData["Title"] = "View Applications";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
    <!-- Bootstrap Icons (Ensure icons display properly) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
</head>

<div class="container-fluid">
    <div class="row">
        <!-- Sidebar -->
        <div class="col-md-3 sidebar bg-primary text-white vh-100 d-flex flex-column">
            <div class="logo text-center py-3">
                <img src="~/img/logo.png" alt="Job Portal" class="img-fluid" style="max-width: 150px;">
            </div>
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a asp-controller="Dashboard" asp-action="ManageJobs" class="nav-link text-white @(ViewData["Title"] == "Manage Jobs" ? "active" : "")">
                        <i class="bi bi-briefcase"></i> Manage Jobs
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-controller="Dashboard" asp-action="AddJob" class="nav-link text-white @(ViewData["Title"] == "Add Job" ? "active" : "")">
                        <i class="bi bi-plus-circle"></i> Add Job
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-controller="Dashboard" asp-action="ViewApplications" class="nav-link text-white active">
                        <i class="bi bi-person"></i> View Applications
                    </a>
                </li>
            </ul>
        </div>

        <!-- Main Content -->
        <div class="col-md-9 bg-light p-4">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h2 class="text-primary">Job Applications</h2>
            </div>
            <p>Here you can review applications submitted for your job postings.</p>

            <div class="table-responsive">
                <table class="table table-striped table-hover text-center">
                    <thead class="bg-dark text-white">
                        <tr>
                            <th>#</th>
                            <th>Applicant Name</th>
                            <th>Job Title</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Count > 0)
                        {
                            int count = 1;
                            @foreach (var application in Model)
                            {
                                <tr>
                                    <td>@count</td>
                                    <td>@application.UserName</td>
                                    <td>@application.JobTitle</td>
                                    <td>
                                        <span class="badge
                                            @(application.Status == "Accepted" ? "bg-success" :
                                              (application.Status == "Rejected" ? "bg-danger" : "bg-warning"))">
                                            @application.Status
                                        </span>
                                    </td>
                                    <td>
                                        <a asp-controller="Dashboard" asp-action="ApplicationDetails" asp-route-id="@application.Id" class="btn btn-info btn-sm">
                                            <i class="bi bi-eye"></i> View
                                        </a>
                                        <a asp-controller="Dashboard" asp-action="DeleteApplication" asp-route-id="@application.Id" class="btn btn-danger btn-sm"
                                           onclick="return confirm('Are you sure you want to delete this application?');">
                                            <i class="bi bi-trash"></i> Delete
                                        </a>
                                    </td>
                                </tr>
                                count++;
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="5" class="text-center text-muted">No applications found.</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
 *@