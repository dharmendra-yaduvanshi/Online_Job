<head>
    <!-- Bootstrap Icons (Ensure icons display properly) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        .nav-link.active, .nav-link:hover {
            background-color: white; /* Darker shade for active and hover */
            border-radius: 8px;
            transition: background-color 0.2s ease-in-out;
        }
    </style>
</head>

<div class="d-flex flex-column vh-100 bg-primary text-white  p-3">
  @*   <h4 class="text-center mb-4">
        <i class="bi bi-speedometer2"></i> Dashboard
    </h4> *@
    <ul class="nav flex-column">
        @* <li class="nav-item">
            <a class="nav-link text-dark @(ViewData["Title"] == "Home" ? "active" : "")" asp-controller="Dashboard" asp-action="Index">
                <i class="bi bi-house-door"></i> Home
            </a>
        </li> *@
        <li class="nav-item">
            <a class="nav-link text-dark @(ViewData["Title"] == "Manage Jobs" ? "active" : "")" asp-controller="Dashboard" asp-action="ManageJobs">
                <i class="bi bi-briefcase"></i> Manage Jobs
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark @(ViewData["Title"] == "Add Job" ? "active" : "")" asp-controller="Dashboard" asp-action="AddJob">
                <i class="bi bi-plus-circle"></i> Add Job
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark @(ViewData["Title"] == "View Applications" ? "active" : "")" asp-controller="Dashboard" asp-action="ViewApplications">
                <i class="bi bi-person"></i> View Applications
            </a>
        </li>
    </ul>
</div>
